function carpricesedited = importfile(filename, dataLines)
%IMPORTFILE Import data from a text file
%  CARPRICESEDITED = IMPORTFILE(FILENAME) reads data from text file
%  FILENAME for the default selection.  Returns the data as a table.
%
%  CARPRICESEDITED = IMPORTFILE(FILE, DATALINES) reads data for the
%  specified row interval(s) of text file FILENAME. Specify DATALINES as
%  a positive scalar integer or a N-by-2 array of positive scalar
%  integers for dis-contiguous row intervals.
%
%  Example:
%  carpricesedited = importfile("C:\Users\ASSIFUAH NATHANIEL\Downloads\Compressed\archive_6\car_prices_edited.csv", [1, 2502]);
%
%  See also READTABLE.
%
% Auto-generated by MATLAB on 24-Apr-2024 14:52:05

%% Input handling

% If dataLines is not specified, define defaults
if nargin < 2
    dataLines = [1, Inf];
end

%% Set up the Import Options and import the data
opts = delimitedTextImportOptions("NumVariables", 13);

% Specify range and delimiter
opts.DataLines = dataLines;
opts.Delimiter = ",";

% Specify column names and types
opts.VariableNames = ["year", "make", "model", "trim", "body", "transmission", "condition", "odometer", "color", "interior", "seller", "mmr", "sellingprice"];
opts.VariableTypes = ["double", "categorical", "categorical", "categorical", "categorical", "categorical", "double", "double", "categorical", "categorical", "categorical", "double", "double"];

% Specify file level properties
opts.ExtraColumnsRule = "ignore";
opts.EmptyLineRule = "read";

% Specify variable properties
opts = setvaropts(opts, ["make", "model", "trim", "body", "transmission", "color", "interior", "seller"], "EmptyFieldRule", "auto");

% Import the data
carpricesedited = readtable(filename, opts);

end